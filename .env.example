# LLM Configuration - Groq (default, free)
# Get your free API key at: https://console.groq.com
LLM_PROVIDER=groq
GROQ_API_KEY=your-groq-api-key-here
# GROQ_MODEL=llama-3.3-70b-versatile

# Alternative LLM providers (for future use)
# Uncomment the provider you want to use and add your API key

# OpenAI (GPT-4o recommended for best results)
# LLM_PROVIDER=openai
# OPENAI_API_KEY=your-openai-api-key
# OPENAI_MODEL=gpt-4o

# Anthropic Claude (Claude 3.5 Sonnet recommended)
# LLM_PROVIDER=anthropic
# ANTHROPIC_API_KEY=your-anthropic-api-key
# ANTHROPIC_MODEL=claude-3-5-sonnet-20241022

# Ollama (local)
# LLM_PROVIDER=ollama
# OLLAMA_BASE_URL=http://localhost:11434
# OLLAMA_MODEL=llama3

# Optional: Set temperature (0.0-1.0, default 0.7)
# LLM_TEMPERATURE=0.7

# BigDeckAppV3 Integration (future)
# For accessing user's card inventory
BIGDECK_API_URL=http://localhost:3000/api
BIGDECK_API_KEY=

# Scryfall API (no key needed - public API)
# Rate limit: 100ms between requests
SCRYFALL_API_URL=https://api.scryfall.com

# Data Source APIs (no keys needed - public/scraped data)
# EDHREC, MTGGoldfish, and Untapped.gg integrations are built-in
